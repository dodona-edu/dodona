<button type="submit" class="btn btn-primary btn-fab <%= "hidden-fab" if defined?(hide) && hide %>" form="<%= form %>">
  <i class="material-icons md-36 material-icons-<%= icon %>"><%= icon %></i>
</button>
