document.getElementById("users-table-wrapper").innerHTML= "<%= escape_javascript(render partial: 'members_table', locals: {course_memberships: @course_memberships}) %>"
dodona.onReady(dodona.initTooltips);
