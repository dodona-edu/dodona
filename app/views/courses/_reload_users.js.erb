<% toast = local_assigns.fetch :toast, nil %>
<% if toast %>
  new dodona.Toast("<%= toast %>");
<% end %>
<% count = @course.pending_members.count %>
document.getElementById("pending-count").innerText = "<%= count if count.nonzero? %>";
dodona.search.search();
