$("#scoresheet-table-wrapper").html("<%= escape_javascript(render partial: 'scoresheet_table', locals: {course: @course, series: @series, users: @users, hash: @hash, total_activity_count: @total_activity_count, total_by_user: @total_by_user}) %>")
