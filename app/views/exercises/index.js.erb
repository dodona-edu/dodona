<% if @series %>
    $("#exercises-table-wrapper").html("<%= escape_javascript(render partial: 'series_exercises_table', locals: {exercises: @exercises, series: @series}) %>");
    dodona.seriesEditExercisesLoaded();
<% else %>
    $("#exercises-table-wrapper").html("<%= escape_javascript(render partial: 'exercises_table', locals: {exercises: @exercises}) %>");
<% end %>
