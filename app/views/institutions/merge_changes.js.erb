document.querySelector("#merge-confirm-info-wrapper").innerHTML = "<%= escape_javascript(render partial: "merge_changes", locals: { institution: @institution, other: @other }) %>";
dodona.switchPanels();
