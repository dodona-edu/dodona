<div class="alert alert-warning">De aanpassing van de remote of het pad zal <b>NIET</b> toegepast worden op de server.</div>

<div class="row">
  <div class="col-sm-10 col-sm-offset-1 col-xs-12">
    <div class="card">
      <div class="card-title card-title-colored">
        <h2 class="card-title-text"><%= @judge.name %></h2>
        <div class="card-title-fab">
          <button type="submit" class="btn btn-primary btn-fab" form="edit_judge_<%= @judge.id %>">
            <span class="glyphicon glyphicon-ok"></span>
          </button>
        </div>
      </div>
      <div class="card-supporting-text">
        <%= render 'form', judge: @judge %>
      </div>
    </div>
  </div>
</div>
