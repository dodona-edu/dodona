<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Dodona", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to(t("layout.menu.exercises"), exercises_path) %></li>
          <li><%= link_to(t("layout.menu.my_submissions"), submissions_path) %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.full_name %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% if policy(User).index? %>
                <li><%= link_to t("layout.menu.users"), users_path %></li>
              <% end %>
              <% if policy(Course).index? %>
                <li><%= link_to t("layout.menu.courses"), courses_path %></li>
              <% end %>
              <% if policy(Judge).index? %>
                <li><%= link_to t("layout.menu.judges"), judges_path %></li>
              <% end %>
              <% if policy(Repository).index? %>
                <li><%= link_to t("layout.menu.repositories"), repositories_path %></li>
              <% end %>
              <% if current_user.zeus? %>
                <li><%= link_to "Job queue", "/dj/overview" %></li>
              <% end %>
              <li><%= link_to t("layout.menu.profile"), current_user %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to t("layout.menu.log_out"), destroy_user_session_path, :method => :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("layout.menu.log_in"), new_user_session_path %></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class='glyphicon glyphicon-globe'></span> <%= I18n.locale.to_s %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'English', url_for( :locale => 'en' ) %></li>
            <li><%= link_to 'Nederlands', url_for( :locale => 'nl' ) %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
