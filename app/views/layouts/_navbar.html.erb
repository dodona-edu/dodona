<nav class="navbar navbar-default navbar-fixed-top navbar-flex" role="navigation">
  <div class="container navbar-flex">
    <button type="button" class="drawer-toggle hidden-lg" data-target="#drawer" aria-expanded="false" aria-controls="drawer">
        <span class="sr-only">Toggle drawer</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>
    <%= link_to root_path, class: "navbar-brand" do %>
      <span class="hidden-xs">Dodona</span>
      <i class="visible-xs material-icons">home</i>
    <% end %>
    <%= render 'layouts/breadcrumbs' %>
    <div class="navbar-spacer"></div>
    <div class="navbar-header navbar-toggle-button">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <i class="material-icons">
          expand_less
        </i>
      </button>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-actions">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="material-icons md-12">account_circle</i><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to t("layout.menu.profile"), current_user %></li>
              <% if !current_user.admin? %>
                <li><%= link_to(t("layout.menu.my_submissions"), submissions_path) %></li>
              <% end %>
              <% if current_user.admin? %>
                <li><%= link_to(t("layout.menu.all_submissions"), submissions_path) %></li>
              <% end %>
              <li role="separator" class="divider"></li>
              <li>
                <% if current_user.institution&.saml? %>
                  <%= link_to t("layout.menu.log_out"), destroy_user_session_path(idp: session[:current_idp]), :method => :delete, target: "_blank", onclick: "window.setTimeout(function () {location.href = '#{root_path}';}, 1000);" %>
                <% else %>
                  <%= link_to t("layout.menu.log_out"), destroy_user_session_path, :method => :delete %>
                <% end %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("layout.menu.log_in"), sign_in_path %></li>
        <% end %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class='material-icons md-12'>language</i><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'English', params.to_unsafe_h.merge(locale: "en") %></li>
            <li><%= link_to 'Nederlands', params.to_unsafe_h.merge(locale: "nl") %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
