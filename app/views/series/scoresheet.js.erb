$("#scoresheet-table-wrapper").html("<%= escape_javascript(render partial: 'scoresheet_table', locals: {course: @course, series: @series, exercises: @exercises, users: @users, submission_hash: @submission_hash}) %>")
