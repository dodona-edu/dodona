<% series_card = render partial: 'series',
                        locals: {
                          series: @series,
                          loaded: true,
                          user: @user,
                        } %>
dodona.replaceHTMLExecuteScripts(document.getElementById("series-card-<%= @series.id %>"), "<%= escape_javascript series_card %>");
dodona.ready.then(dodona.initTooltips)
