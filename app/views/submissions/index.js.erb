$("#submissions-table-wrapper").html("<%= escape_javascript(render partial: 'submissions_table', locals: {submissions: @submissions, exercise: @exercise}) %>");
if (dodona.feedbackTableLoaded) {
    dodona.feedbackTableLoaded();
}
