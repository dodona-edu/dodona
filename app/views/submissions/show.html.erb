<%= render 'navbar_links' %>
<div class="row">
  <div class="col-sm-10 col-sm-offset-1 col-xs-12">
    <div class="card">
      <div class="card-title card-title-colored">
        <h2 class="card-title-text"><%= t ".submission" %> <%= @submission.exercise.name %><%= " - #{@submission.course.name}" unless @submission.course.nil? %></h2>
      </div>
      <div class="card-supporting-text">
        <%= render partial: 'description', locals: { submission: @submission } %>
      </div>
    </div>
  </div>
</div>
<% if @submission.queued? || @submission.running? %>
<script>setTimeout(function () { location.reload(1); }, 3000);</script>
<% end %>
