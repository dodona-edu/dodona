$("#submission-wrapper").html("<%= escape_javascript(render partial: 'description', locals: {submission: @submission}) %>");
if (dodona.feedbackLoaded) {
    dodona.feedbackLoaded(<%= @submission.id %>);
}
